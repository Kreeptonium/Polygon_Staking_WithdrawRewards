import { BridgeClient, IPOSClientConfig } from "@maticnetwork/maticjs";
import { MaticToken, StakeManager, ValidatorShare } from "./contracts";
export declare class StakingClient extends BridgeClient<IPOSClientConfig> {
    stakeManager: StakeManager;
    maticToken: MaticToken;
    init(config: IPOSClientConfig): Promise<void>;
    validatorShare(address: string): ValidatorShare;
}
